- 1. 優化 Scrollbar 顯示：
  - 將 Scrollbar 加回來，並使用 Customize 樣式，做成比較 Modern 的感覺，而非系統內建樣式
- 2. 在進入或離開 Grid 模式的時候，應該要自動捲動到目前圖片所在的位置才對。
  - 目前每次進入 Grid 模式，它都會跳到最上面，這樣是不對的。
- 3. trackpad pinch zoom 在高 loading 時會被中斷
- 4. 有顯示捲軸之後，要加入支援 grid 狀態下的 pageup, pagedown 操作
